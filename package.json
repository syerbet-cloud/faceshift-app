{
  "name": "faceshift-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "mongoose": "^7.0.3",
    "bcrypt": "^5.1.0",
    "jsonwebtoken": "^9.0.0",
    "multer": "^1.4.5-lts.1",
    "stripe": "^11.0.0",
    "axios": "^1.3.4"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}

git init                 # inicializa o repo local
git add .                # adiciona todos os arquivos
git commit -m "Primeiro commit"  # faz o commit
git branch -M main       # garante que a branch principal é main
git remote add origin https://github.com/syerbet-cloud/faceshift-app.git  # linka com o GitHub
git push -u origin main  # envia para o GitHub
